#!/usr/bin/ruby

$LOAD_PATH.unshift "test"
$LOAD_PATH.unshift "lib"

require "minitest/autorun"
require "minitest/path_expander"

pe = Minitest::PathExpander.new

files = pe.process

if files.empty? then
  pe.args << "test"
  files = pe.process
end

files.each do |f|
  require "./#{f}"
end
